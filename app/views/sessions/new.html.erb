<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Form</title>
  <style>
    /* Đặt form vào giữa màn hình */
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f4f4f4;
    }
    /* Nút Home ở góc trên bên phải */
    .home-btn {
      position: absolute;
      top: 20px;
      right: 20px;
      padding: 10px 20px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }

    .login-form-container {
      background-color: #fff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }

    /* Các input */
    .form-input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
      box-sizing: border-box;
    }

    /* Nút đăng nhập */
    .submit-btn {
      background-color: #4CAF50;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      width: 100%;
      margin-top: 10px;
    }

    .submit-btn:hover {
      background-color: #45a049;
    }

    /* Link forgot password */
    .forgot-password-link {
      display: block;
      margin-top: 15px;
      color: #007BFF;
      text-decoration: none;
    }

    .forgot-password-link:hover {
      text-decoration: underline;
    }

    /* Cảnh báo và thông báo */
    .alert, .notice {
      text-align: center;
      margin-bottom: 10px;
    }

    .alert {
      color: red;
    }

    .notice {
      color: green;
    }
  </style>
</head>
<body>

  <div class="login-form-container">
    <!-- Hiển thị thông báo cảnh báo nếu có -->
    <div class="alert">
      <!-- Thông báo lỗi hoặc cảnh báo -->
      <%= tag.div(flash[:alert], style: "color:red; text-align:center;") if flash[:alert] %>
    </div>

    <!-- Hiển thị thông báo thành công nếu có -->
    <div class="notice">
      <%= tag.div(flash[:notice], style: "color:green; text-align:center;") if flash[:notice] %>
    </div>

    <!-- Form đăng nhập -->
    <%= form_with url: session_path, html: { class: "login-form" } do |form| %>
      <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: "Enter your email address", class: "form-input" %>
      <%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: "Enter your password", maxlength: 72, class: "form-input" %>
      <%= form.submit "Sign in", class: "submit-btn" %>
    <% end %>

    <!-- Link quên mật khẩu -->
    <%= link_to "Forgot password?", new_password_path, class: "forgot-password-link" %>
  </div>

</body>
</html>
